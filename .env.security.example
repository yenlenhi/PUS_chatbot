# Cấu hình Bảo mật
# ==================

# JWT Authentication
# Secret key để mã hóa JWT tokens (THAY ĐỔI KEY NÀY TRONG PRODUCTION!)
# Tạo key mạnh: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-key-change-this-in-production-use-openssl-rand-hex-32

# Thuật toán mã hóa JWT
JWT_ALGORITHM=HS256

# Thời gian hết hạn token (phút)
ACCESS_TOKEN_EXPIRE_MINUTES=30


# HTTPS Configuration
# ==================

# Bật chế độ HTTPS only (redirect HTTP -> HTTPS)
HTTPS_ONLY=false

# Phiên bản TLS tối thiểu
TLS_MIN_VERSION=1.2


# Checksum Verification
# =====================

# Bật kiểm tra checksum cho upload endpoints
ENABLE_CHECKSUM_VERIFICATION=false

# Thuật toán checksum (md5 hoặc sha256)
CHECKSUM_ALGORITHM=sha256


# CORS Configuration
# ==================

# Origins được phép truy cập API
# Development: * (cho phép mọi origin)
# Production: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=*
